Drop your own images in this folder (e.g. `1.jpg`, `2.jpg`, ...).

Then in `config.js`, point each entry's `src` at the file, e.g.:

```js
{ src: "photos/1.jpg", label: "Beach Day" }
```

Nothing has to live here, though — `src` also accepts any image URL.
